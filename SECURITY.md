# 安全策略

## 支持范围

首次公开版本发布后，仅维护默认分支的最新版本。历史版本不承诺安全更新。

## 报告漏洞

请勿在公开 Issue、讨论区、日志或示例文件中披露漏洞细节、凭据、真实文档或个人信息。远程仓库创建后，维护者应启用 GitHub 私密漏洞报告；在该渠道启用前，请通过维护者公开资料中列出的私密联系方式报告。

报告应尽量包含受影响版本、复现条件、影响范围和最小化复现步骤，不要附带真实业务数据。维护者确认收到后会评估严重性、制定修复和披露计划。

## 部署边界

DocWeave 当前没有用户认证、权限隔离、租户隔离或公网限流能力。默认 Compose 配置仅监听 `127.0.0.1`，仅适合本机或具有额外访问控制的可信网络。未经安全加固，不应直接暴露到公网。

LLM API Key 仅应通过后端环境变量或密钥管理服务注入。所有外部模型服务应使用可信 HTTPS 连接；不得提交 `.env`、访问令牌、私钥、生产连接串、用户上传文档或运行数据库。
